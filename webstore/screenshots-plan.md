# Chrome Web Store Screenshots Plan

Provide at least 3 images (1280x800 or 1280x720) and one small tile (440x280). PNG recommended.

1. Landing/Hero (Dark):
   - Source: docs/assets/screenshot.png or docs/assets/hero.webp (capture still)
   - Text overlay: "Visualize message flows in DevTools"

2. Pretty JSON Option:
   - Show the popup with "Pretty JSON" checkbox enabled
   - Console shows prettified JSON payload
   - Include arrow/group styling

3. Channels Coverage:
   - Grid showing window.postMessage, MessageChannel, BroadcastChannel, Worker, ServiceWorker events grouped
   - Use Docs or Playground for staged capture

4. Small Tile (440x280):
   - Use `docs/assets/logo.svg` with gradient background

Tips:
- Use consistent dark theme for visual cohesion
- Capture at 2x scale then downsize for sharper images
- Avoid personally identifiable information in captures